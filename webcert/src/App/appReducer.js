const inititalState = {};

const appStateReducer = (state, action) => {
  switch (action.type) {
    default:
      return { ...state };
  }
};

export { inititalState };
export default appStateReducer;
